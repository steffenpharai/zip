# Sudoers entry for ROS 2 installation
# Add this line to /etc/sudoers.d/zip-ros2 (create the file if it doesn't exist)
# 
# IMPORTANT: Use 'visudo -f /etc/sudoers.d/zip-ros2' to edit safely
# This will validate syntax before saving

zip ALL=(ALL) NOPASSWD: /usr/bin/apt, /usr/bin/apt-get, /usr/bin/apt-key, /usr/bin/rosdep, /usr/bin/locale-gen, /usr/bin/tee, /bin/rm, /usr/bin/curl

# Alternative: If you want to allow all apt-related commands more broadly:
# zip ALL=(ALL) NOPASSWD: /usr/bin/apt*, /usr/bin/rosdep, /usr/bin/locale-gen, /usr/bin/tee, /bin/rm, /usr/bin/curl

# To apply:
# 1. sudo visudo -f /etc/sudoers.d/zip-ros2
# 2. Paste the line above
# 3. Save and exit
# 4. Test with: sudo -n apt update
