// secrets.example.h — template. Copy to `secrets.h` and fill in.
// `secrets.h` is gitignored.

#pragma once

#define ZIP_WIFI_SSID      "your-home-wifi-ssid"
#define ZIP_WIFI_PASSWORD  "your-home-wifi-password"
