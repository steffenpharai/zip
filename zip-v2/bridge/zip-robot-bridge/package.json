{
  "name": "zip-robot-bridge",
  "version": "2.0.0",
  "description": "Bridge service for ZIP robot - translates WebSocket to ELEGOO-style JSON serial protocol",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:local": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "dev:local": "LOOPBACK_MODE=true tsx watch src/index.ts",
    "dev:loopback": "LOOPBACK_MODE=true tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test:health": "node -e \"fetch('http://localhost:8766/health').then(r => r.json()).then(d => console.log(JSON.stringify(d, null, 2))).catch(e => console.error('Health check failed:', e.message))\"",
    "test:integration": "tsx scripts/test-integration.ts",
    "test:loopback": "concurrently --kill-others -s first \"LOOPBACK_MODE=true tsx src/index.ts\" \"sleep 2 && tsx scripts/test-integration.ts\"",
    "test:smoke": "tsx scripts/drive-smoke.ts",
    "test:sensor": "tsx scripts/sensor-smoke.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "serialport": "^13.0.0",
    "ws": "^8.18.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/ws": "^8.5.10",
    "concurrently": "^8.2.2",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
